<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sellinghouses.salescontrol.module.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.sellinghouses.salescontrol.module.user.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="wx_openid" property="wxOpenid"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="primary_role_code" property="primaryRoleCode"/>
        <result column="status" property="status"/>
        <result column="last_login_time" property="lastLoginTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="UserColumns">
        id,
        username,
        password,
        real_name,
        phone,
        wx_openid,
        avatar_url,
        primary_role_code,
        status,
        last_login_time,
        create_time,
        update_time
    </sql>

    <select id="selectById" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE username = #{username}
        LIMIT 1
    </select>

    <select id="selectByPhone" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE phone = #{phone}
        LIMIT 1
    </select>

    <select id="selectByWxOpenid" resultMap="UserResultMap">
        SELECT
        <include refid="UserColumns"/>
        FROM `user`
        WHERE wx_openid = #{wxOpenid}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.sellinghouses.salescontrol.module.user.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (
            username,
            password,
            real_name,
            phone,
            wx_openid,
            avatar_url,
            primary_role_code,
            status
        ) VALUES (
            #{username},
            #{password},
            #{realName},
            #{phone},
            #{wxOpenid},
            #{avatarUrl},
            #{primaryRoleCode},
            #{status}
        )
    </insert>

    <update id="updateLastLoginTime">
        UPDATE `user`
        SET last_login_time = NOW(),
            update_time = NOW()
        WHERE id = #{id}
    </update>
</mapper>
